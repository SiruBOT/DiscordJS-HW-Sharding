const { Client } = require('discord.js')
class ShardingClient extends Client {
  constructor (client, shardingOptions) {

  }

  /** */
  chkValidOptions (opt) {

  }
}

module.exports = ShardingClient
